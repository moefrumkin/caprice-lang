(* TEST
  include refute;
*)


let is_nonempty ls =
  match ls with
  | [] -> false
  | _ -> true
  end

let rec append_list (type a') (a : list a') (b : list a') : { ls : list a' | is_nonempty ls } =
  match a with
  | [] -> b (* ERROR: `b` could be empty *)
  | hd :: tl -> hd :: append_list a' tl b
  end
