(* TEST
  include exhaust;
*)

let is_nonempty ls =
  match ls with
  | [] -> false
  | _ -> true
  end

let rec append_list (type a') (a : list a') (b : list a' | is_nonempty b) : { l : list a' | is_nonempty l } =
  match a with
  | [] -> b
  | hd :: tl -> hd :: append_list a' tl b
  end
