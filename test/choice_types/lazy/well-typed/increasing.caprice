(* TEST
    include exhaust;
*)

let true_type = { b : bool | b }
let pos = { n : int | n > 0 }

let is_increasing (f : int -> int) -> bool =
    let n = anyvalue in
    ((f n) > n) |> true

let add_n (n : pos) : {f : int -> int | is_increasing f} =
    fun m -> m + n